# ============================================================
# coc7th-keeper .gitignore — 发布红线：密钥 / 授权 PDF / 玩家数据
# 绝不提交的内容（提交前用 git ls-files 复核）：
#   .dsh/backup/（含飞书 appSecret）、模组/（原版 PDF 与转换源稿）、
#   coc-session + coc-session-*/（玩家运行时数据）
# 内置模组（the-haunting、toy-dancer-comes）随仓库分发（见根 README 版权说明）。
# ============================================================

# ---- 机密与凭证（绝不能入库）----
.dsh/backup/

# ---- 授权模组源材料（原版 PDF 与转换源稿，不入公共仓库）----
模组/

# ---- 运行时玩家数据（不入库）----
coc-session/
coc-session-*/

# ---- Python 缓存与日志 ----
__pycache__/
*.pyc
*.log
.dsh/bin/*.log*

# ---- 依赖与构建产物 ----
node_modules/
dist/

# ---- AgentTeams 团队状态文件（含任务契约，不入库）----
.agent-teams/
