# 完成报告 · M8R7 模组引入体验与表侧隐私（sleeping-cat 实测转化）

> 承接 [`任务书-M8R7-模组引入与表侧隐私-20260904-0747.md`](任务书-M8R7-模组引入与表侧隐私-20260904-0747.md)｜2026-09-04（北京时间）｜改动留工作区未提交
> 修复前实测截图存档：`.tmp/round-shots/01–06`（未入仓）；修复后复测截图：`07–08`。

## 1. 实测过程（修复前）

浏览器（Vite 5173 + 当时运行中的旧后端，后重启为 HEAD 代码）+ 真实 LLM（OpenCode Go / mimo-v2.5）以预制角色（雫）对 sleeping-cat 开团，完成 4 次结算、逐轮截图：

| 存档 | 内容 | 结论 |
|---|---|---|
| 01 | 开场（第 0 轮） | 场景栏 + 开场卡 = KP summary 原文（怪物身份 / SAN 1/1D4 /【跳跃】检定全亮给玩家）；无钩子；无手卡 |
| 02–05 | 第 0–3 轮结算 | AI 叙事本身合规（尸体只作感官描写、黑影不点名、失败措辞"没能看出更多端倪"）；私密感知定向正确；第 3 轮出现 fallback 兜底叙事（`（场景 …）` 前缀） |
| 06 | 内容页 | meta.summary + 场景名链整体剧透（冷蛛 / 幻梦境 / 石像归还通关路线） |

API 取证：`/api/modules/sleeping-cat/scenes` 返回全部场景 `summary/checks/clues/npcs/next`；`/api/modules` meta 含 `summary/tags/source`。

**附带发现**：非房主玩家点「推进回合」必 401——`client.ts` `advanceRound` 写死 `X-Host-Token`，与 M8R5"人人可点"定案矛盾（后端 `_identify_requester` 本就接受玩家令牌）。

## 2. 修复内容

**后端（投影发生在"玩家边界"，AI 上下文仍读全量）**
- `server/modules.py`：新增 `SCENE_PUBLIC_FIELDS` + `public_scene()` / `public_scenes()`（场景投影）与 `META_KP_ONLY_FIELDS` + `public_meta()` / `public_modules()`（meta 剥 `summary/tags/source`）。
- `server/api/modules.py`：`/modules`、`/modules/{id}`、`/modules/{id}/scenes(/{sid})` 全部改走投影。
- `server/api/games.py` `_inject_opening`：开场白 = `intro`（缺失仅「场景名 · 地点」表头，绝不回退 summary）；首场景 `handouts` 以 handout 消息随开场注入叙事流。
- `server/state_apply.py` `_apply_scene`：返回投影 → `scene_changed` 广播不再携带 `summary/checks/clues/npcs`；`server/roundman.py` `scene_change_payload` 同步（一致性维护，该函数当前无调用方）。
- 契约备注：管线内 adjudicate/narrate 收到的 `scene` 仍来自 `modules.get_scene` 全量对象——KP（AI）视角无损，符合 gm_system §6。

**前端（只展示表侧内容）**
- `frontend/src/types.ts`：`SceneInfo` 改为投影形状（`intro?`）；`SceneChangedEvent.scene` 去 `summary`；`ModuleInfo` 增 `public_summary?`。
- `frontend/src/stores/game.ts`：`scene_changed` 消息文本 = 表头 + `intro`（无则仅表头）。
- `frontend/src/views/Play.vue` + `components/SceneBar.vue`：场景栏 brief 用 `intro`（prop 更名 `summary`→`brief`）。
- `frontend/src/views/Content.vue`：简介展示 `public_summary`（缺失显示"剧情简介对玩家保密"占位）；场景名链（剧透链）不再展示，仅场景数。
- `frontend/src/api/client.ts` `advanceRound`：`host: true` → `host: false`（玩家令牌；房主在创建房间的浏览器里 playerToken 与 hostToken 同值，同样通过）→ 非房主推进 401 修复。

**模组数据（4 个模组，E3n 追加指令后全量覆盖）**
- `modules/sleeping-cat/`：8 个场景全部新增 `intro`（玩家可见入场白：为什么来=『重要之人』担心短信+红叶远足 / 怎么来的=上行缆车 / 现状=雾越来越浓，收在可行动状态）；场景名表侧化（`引开冷蛛 / 结缘之钟`→`结缘之钟`、`冷蛛袭来 / 石像归位`→`石像归位`、`终末 / 切断连接`→`尾声` 等）；`meta.json` 新增 `public_summary`。summary/checks（KP 视角）原样保留供 AI 使用。
- **`the-haunting/`（追加复查）**：intro ×7；场景名表侧化 2 处（`地下室（高潮）`→`地下室`、`收尾`→`风暴过后`）；`public_summary`。
- **`toy-dancer-comes/`（追加复查）**：intro ×10；场景名表侧化 5 处（`园区内的众人（NPC）`→`园区里的众人`、`众人目送 / 我要逃跑`→`众人的目送`、`突破围堵 / 快逃`→`墙上的血字`、`回头客 / 肉体改造`→`再次登门`、`最终战：午夜钟声为谁鸣`→`午夜钟声`）；`public_summary`。
- **`yuren-pie/`（追加复查）**：intro ×4；场景名表侧化 4 处（`酒吧续摊 / 共建住宅`→`酒吧 / 回家续摊`、`开门 / 异界洞穴`→`推开门`、`遭遇深潜者 / 战斗与追逐`→`洞穴深处的阴影`、`派 / 收尾`→`剩下的派`）；`public_summary`。KP 自定义字段（`random_hazards` / `chase_map`）由投影自动剥除，不进玩家视图。

intro 统一遵守写作规范四条自检：只写玩家可感知的（不点怪物真名、不预告检定与数值、不发明线索）；交代为什么来/怎么来/现在在哪；收在可行动状态；第二人称约 100–300 字（尾声类短场景除外）。

**测试**
- 新增 `server/tests/test_module_privacy.py` 6 例：scenes 投影无 KP 关键词（冷蛛/幻梦境/检定/SAN 1/归还台座）、meta 投影 + public_summary 齐备、开场白来自 intro + 手卡注入、scene 变动返回投影、**全模组** intro 覆盖、**全模组** name/location/intro 表侧化禁用词断言（各模组独立禁用词表：the-haunting 禁"鬼魂/德拉波尔/高潮/收尾"、toy-dancer 禁"献祭/恶灵/谢尔顿/木偶化/最终战/肉体改造/NPC"、yuren-pie 禁"深潜者/异界/收尾/遭遇"）。
- `server/tests/test_api.py` `test_modules_api`：场景接口断言从"含 npcs"改为"只含表侧字段集"+ meta 无 summary/source。

**文档**
- `docs/模组拆解说明.md`：§3.1 meta 字段表新增 `public_summary`（强烈推荐）并标注 `summary/tags/source` 为 KP 视角；§3.6 场景字段表新增 `intro` 与 **intro 写作规范**（四条自检：交代为什么来/怎么来/现在在哪、只写可感知、收在可行动状态、100–300 字）+ 场景 `name`/`location` 表侧化命名规则 + 程序契约补"玩家边界投影"；§4 开局注入行更新（intro + 手卡）；§6 校验清单新增 4 项（含"玩家视角通读 /api/modules/{id}/scenes"终检）；§7 样例补 sleeping-cat（参照实现）。

## 2.5 复检与防回归加固（E3n 追加指令：确认模组全部更新到位、拆解说明可保证后续适配）

- **机器化复检**：四模组 `validate` + intro/public_summary/handout 路径逐项预检全过；玩家同源 API 通读 29 场景零泄漏；yuren-pie 的 KP 自定义字段确认被投影剥除；跨模组 e2e——the-haunting 实开新房（627502ce），开场消息 = 表头 + intro（老友来信钩子），无 KP summary。
- **修复三处文档-代码漂移**：
  1. §3.1 / §3.6 的 JSON **示例模板补 `public_summary` / `intro`**——示例是未来拆模组的复制源，此前照抄会产出不合规结构；
  2. §1「隐私分级」表述过时（原写"scenes.json 玩家可看"），改为"表侧字段 vs KP 字段 + 投影"的准确口径；
  3. **`validate_module` 新增三项程序强制**：场景缺 `intro`、meta 缺 `public_summary`、场景 `handouts` 路径不存在 → 报错。配套新测试：构造坏模组目录断言三项错误全部报出 + 已发布模组零误伤。§6 清单相应标注 🤖 自动校验项，"程序只校验存在性，内容质量靠人工"的边界写明。
- 结论：后续新模组只要过 `validate_module`（pytest 长期看护）即**结构上必然适配**当前平台（投影/开场/手卡链路），内容合规由 §6 人工项与全模组禁用词测试看护。

## 3. 门禁（真实数字）

| 门禁 | 结果 |
|---|---|
| `pytest --basetemp=新目录 -q` | **111 passed**（M8R6 基线 104 + 本轮净增 7） |
| `cd frontend && npm run build` | exit 0（vue-tsc 0 错） |
| `.tmp/linkcheck.py` | **76 OK / 0 断链** |

## 4. 浏览器复测（修复后，新房间 6bbaf68c）

1. 开场：场景栏与开场卡显示沉浸式 intro（红叶/担心短信/缆车/雾），两张玩家资料手卡自动出现在叙事流（截图 07）。
2. 非房主玩家（邀请链接加入）点「推进回合」→ 后端日志 `结算开始（skip_missing=False）` → 结算完成进入第 1 轮（401 修复实证）。
3. `/api/modules/sleeping-cat/scenes` 仅返回 `{id, name, location, intro, handouts}`，场景名无 KP 词。
4. 内容页：无剧透 public_summary + 场景数（截图 08）。
5. **全模组终检（E3n 追加指令）**：以玩家同源 API 通读四模组全部 29 个场景（name/location/intro/handouts），确认无怪物真名、无机制词、无结局词、无未揭示信息；pytest 全模组断言长期看护。

## 5. 已知遗留 / 建议（不阻塞本轮）

1. **旧房间不可逆**：修复前创建的房间，其历史 scene 消息仍是旧 summary 文本（存档数据，未做迁移）。
2. 实测中第 3 轮出现一次 narrate fallback（`（场景 …）` 前缀的兜底文案）——fallback 叙事质量可另行优化；本轮未动。
3. 实测第 2 轮目击茧尸（SAN 1/1D4 场景）AI 未主动安排理智检定——属 AI 裁判质量调优（提示词/场景 checks 注入强度），非本轮隐私边界范畴，建议进入候选池。
4. adjudicate 曾输出英文技能名（Library Use / Spot Hidden，旧后端），新后端轮次已用中文（侦查）；如再现身可在提示词强调中文技能名。
5. `roundman.scene_change_payload` 当前无调用方（M2 遗留），本轮按契约同步为投影；后续可清理。

## 6. 验证方式声明

- 浏览器操作：真实 Chromium（ZCode 内置浏览器）实际点击/输入，非纯 API 模拟；结算为真实 LLM 在线调用（backend 日志 `chat/completions 200` 佐证）。
- 沙箱限制项：无 Docker 构建（沿用 README 待办）；`.ps1` 启停未实跑（本轮未改脚本）。
